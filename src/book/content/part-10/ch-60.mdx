---
title: 'SVG and bitmap import'
part: 'Part X — Tooling and Ecosystem'
partNumber: 10
order: 2
subtitle: 'Vector staging, raster thresholding, cropping, tracing, and production cleanup'
---

_Content coming soon._ SVG import remains the vector route; bitmap import adds crop and
threshold controls that generate editable path code. Both workflows end with simplification,
resampling, stitch-mode choice, travel review, and a real sewability check.

<Run>{`import std.pathops.simplifypath as simplify

// Imported geometry is ordinary path data: refine it before sewing
let outline = trace [ repeat 4 [ fd 20 rt 90 ] ]
let clean = simplify(resample(outline, 1), 0.5)
sewpath(clean)`}</Run>

<Checkpoint chapterId="ch-60">Chapter content coming soon.</Checkpoint>
